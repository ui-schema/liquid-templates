<{{# options.isHeader }}header{{/ options.isHeader  }}{{^ options.isHeader }}section{{/ options.isHeader }} data-ui-schema="uischema.org/Hero" class="ui-hero">
    {{# image }}
        <div class="ui-hero-image">{{> Image }}</div>
    {{/ image }}

    <div class="ui-container">
        {{# heading }}
            {{# options.isHeader }}
                <h1 class="ui-hero-heading">{{ . }}</h1>
            {{/ options.isHeader }}
            {{^ options.isHeader }}
                <h2 class="ui-hero-heading">{{ . }}</h2>
            {{/ options.isHeader }}
        {{/ heading }}
        
        {{# subheading }}
            <h3 class="ui-hero-subheading">{{ . }}</h3>
        {{/ subheading }}

        <div class="ui-hero-details">
            {{# body }}
                <div class="ui-hero-body">{{{ . }}}</div>
            {{/ body }}

            {{# link }}
                <div class="ui-hero-link">{{> Link }}</div>
            {{/ link }}
        </div>
    </div>
</section>
