<div
    data-schema="http://uischema.org/Dropdown"
>
    <input
        type="checkbox"
    >
    <label>
        {{ self.text }}
    </label>
    <ul>
        {% for option in self.options %}
            <li>
                {% if option.@type == 'Link' %}
                    {% include Link.liquid data=option %}
                {% elif option.@type == 'Option' %}
                    {% include Option.liquid data=option %}
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</div>
