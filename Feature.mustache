<div data-ui-schema="uischema.org/Feature" class="ui-feature">
    <div class="ui-feature-image">
        {{# image }}
            {{> Image }}
        {{/ image }}
    </div>

    <div class="ui-feature-details">
        {{# heading }}
            <h3 class="ui-feature-heading">{{ . }}</h3>
        {{/ heading }}
        
        {{# subheading }}
            <h4 class="ui-feature-subheading">{{ . }}</h4>
        {{/ subheading }}
        
        {{# body }}
            <div class="ui-feature-body">{{{ . }}}</div>
        {{/ body }}

        {{# link }}
            <div class="ui-feature-link">{{> Link }}</div>
        {{/ link }}
    </div>
</div>
